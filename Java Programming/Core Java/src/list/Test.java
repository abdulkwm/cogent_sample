package list;

public class Test {
    public static void main(String[] args) {
        String st1 = "Abdu";
        String strNew = st1.substring(2,st1.length()-1);
        int num = st1.length();
        System.out.println(strNew);
    }
}
